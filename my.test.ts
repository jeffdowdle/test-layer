import { expect, describe, it } from '@jest/globals'

describe('asdasd asdasd', () => {
  it('displays status as "Closed" when current date is after start date and after end date', () => {
    expect(true).toEqual(true)
  })
})
